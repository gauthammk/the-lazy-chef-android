<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thelazychef">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NutritionAnswer"/>
        <activity android:name=".RecipesResult" />
        <activity android:name=".PairingsResult" />
        <activity android:name=".NutritionQA" />
        <activity android:name=".WinePairings" />
        <activity android:name=".FindRecipes" />
        <activity android:name=".SearchByIngredients" />
        <activity android:name=".ExploreActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>